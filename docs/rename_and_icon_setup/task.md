# タスク: アプリ名の変更とアイコンの設定

## 概要
アプリ名を「NoFap AI Support」から「リブート！」に変更し、ユーザーから提供された画像をファビコンおよびアプリアイコンとして設定する。

## タスクリスト
- [x] アイコン用画像の配置 (app/icon.jpg)
- [x] `app/layout.tsx` のメタデータ変更 (title, appleWebApp.title)
- [x] `public/manifest.json` の変更 (name, short_name)
- [x] ロゴコンポーネントがある場合、表示名の変更 (主要なUIページには該当なし)
