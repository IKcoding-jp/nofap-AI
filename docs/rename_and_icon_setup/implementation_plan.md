# 実装計画: アプリ名変更とアイコン設定

## 目的
ブランド名を「リブート！」に統一し、提供された画像をアイコンとして使用することで、アプリの独自性を高める。

## 変更内容
1. **アイコンの配置**:
   - アップロードされた画像 (`uploaded_image_1767250511956.jpg`) を `app/icon.jpg` にコピーする。
   - これにより、Next.js が自動的に各サイズのファビコンやアプリアイコンを生成する。

2. **メタデータの更新**:
   - `app/layout.tsx`: `title` を「リブート！」に更新し、`appleWebApp.title` も同様に更新する。
   - `public/manifest.json`: `name` と `short_name` を「リブート！」に更新する。

3. **UI要素の確認**:
   - ヘッダーやナビゲーションバーなどに「NoFap AI」という文字列が表示されている箇所があれば更新する。

## 確認事項
- ブラウザのタブに新しいアイコンが表示されているか。
- ブラウザのタイトルが「リブート！」になっているか。
- マニフェストファイルが正しく読み込まれているか。
