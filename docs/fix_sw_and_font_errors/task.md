# タスクリスト：Service Worker とフォント設定の修正

Service Worker によるキャッシュの不整合解消と、フォントプリロード警告の解決を行います。

## 完了条件
- [ ] `public/sw.js` が更新され、HTML などのナビゲーション要求に対してネットワーク優先（Network First）で動作するようになっている。
- [ ] `app/globals.css` で `body` にフォントが適用され、プリロード警告が解消されている。
- [ ] PWA としての基本機能が維持されている。

## タスク
- [x] 現状の分析と原因特定
- [x] `public/sw.js` の修正（キャッシュ戦略の改善）
- [x] `app/globals.css` の修正（フォント適用の追加）
- [x] 修正内容の確認とドキュメント作成
